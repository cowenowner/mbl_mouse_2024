Given python's hierarchical structure, it probbaly makes most sense to put the individual library .py files (e.g. IO.py) into the current CowenLib/IO folder. That way I can ...
import CowenLib.IO as cio or something like this once CowenLIb is in the python path.
